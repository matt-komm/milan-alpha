language: cpp
matrix:
  include:
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources: ['ubuntu-toolchain-r-test']
          packages: ['g++-4.7']
      env: COMPILER=g++-4.7 CONFIG=Debug

    - os: linux
      compiler: gcc
      addons:
        apt:
          sources: ['ubuntu-toolchain-r-test']
          packages: ['g++-4.9']
      env: COMPILER=g++-4.9 CONFIG=Debug
      
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources: ['ubuntu-toolchain-r-test']
          packages: ['g++-5.0']
      env: COMPILER=g++-5.0 CONFIG=Debug
      
    - os: linux
      compiler: gcc
      addons:
        apt:
          sources: ['ubuntu-toolchain-r-test']
          packages: ['g++-5.3']
      env: 
        - COMPILER=g++-5.3 CONFIG=Debug
        - COMPILER=g++-5.3 CONFIG=Release
      
    - os: linux
      compiler: clang
      env: COMPILER=clang++-3.3 CONFIG=Debug
      
    - os: linux
      compiler: clang
      env: 
        - COMPILER=clang++-3.5 CONFIG=Debug
        - COMPILER=clang++-3.5 CONFIG=Release
      
    - os: osx
      compiler: clang
      env: COMPILER=clang++-3.3 CONFIG=Debug
      
    - os: osx
      compiler: clang
      env: 
        - COMPILER=clang++-3.5 CONFIG=Debug
        - COMPILER=clang++-3.5 CONFIG=Release
      

before_script:
  - export BASEDIR=$PWD
  - mkdir build
  - cd build
  - cmake -D CMAKE_BUILD_TYPE=$CONFIG -D ENABLE_TEST=ON -D CMAKE_INSTALL_PREFIX=$BASEDIR/release ..

script: 
  - cmake --build . --config $CONFIG --target install
  - export LD_LIBRARY_PATH=$BASEDIR/release/bin:$BASEDIR/release/bin/tests:$BASEDIR/release/lib:$LD_LIBRARY_PATH
  - $BASEDIR/release/bin/printSimdSupport
  - ctest -C $CONFIG || ctest -V -C $CONFIG
  
branches:
  only:
    - master
    
notifications:
  email:
    recipients:
      - mkomm@cern.ch
    on_success: change
    on_failure: always

